<div class="page-header" style="border: none">
  <h3><%=  I18n.t('activerecord.models.estado_conservacao') %></h3>
  <hr/>
  <%= link_to "Novo", new_estado_conservacao_path, class: "btn btn-primary btn-sm", data: { tooltip: true, placement: 'top', animation: true }, title: 'Novo registro' %>
</div>

<%= grid(@estado_conservacoes_grid) do |g|
    g.column name: 'Descricao', attribute: 'descricao' do |task|
    task.descricao
  end
    g.column do |task|
    btn = link_to_show(task)
    btn << link_to_edit(edit_estado_conservacao_path(task))
    btn << link_to_delete(task)
    btn
    content_tag :div, btn, class: 'btn-group', role:'group'
  end
end -%>
